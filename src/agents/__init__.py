"""MaydaIQ agent implementations.

Each agent exposes a small deterministic run method. If a Microsoft Agent
Framework runtime is added later, these classes are the narrow place to wrap
them without changing the orchestration contract.
"""

